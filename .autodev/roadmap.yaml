version: 1
priority:
  # Safety gate before another product wave: every current test project and the
  # Android/Windows product shapes must actually run for relevant changes.
  - issue: 323

  # M1 — Frictionless, Trustworthy Daily Task Flow.
  # Capture quickly, complete safely, expose the advanced editor, then make
  # recurrence mean what users actually ask for: obligations within flexible
  # periods rather than accidental fixed dates. Automatic prompting remains a
  # separate stochastic cadence that selects from current eligibility at delivery.
  - issue: 308
  - issue: 309
  - issue: 316
  - issue: 351
  - issue: 334

  # M2 — Explainable Decision Support.
  - issue: 311
  - issue: 312
  - issue: 313
  - issue: 314

  # M2.5 — Hierarchical scheduling for coherent bodies of executable work.
  # TaskCollections are scheduling containers, not projects/workflow graphs:
  # score the collection once at the top level, then score eligible members
  # internally. Simple blockers gate schedulability only; richer dependency
  # semantics remain Streamline-owned.
  - issue: 353
  - issue: 354
  - issue: 355

  # M3 — Find, Act, and Scale the daily loop.
  - issue: 310
  - issue: 315
  - issue: 317
  - issue: 318

  # Recompose first-run only after the title-first capture, editor, no-eligible,
  # and V2 Now/Tasks/Settings foundations it consumes are available.
  - issue: 347

  # M4 — Coherent and Accessible Product.
  - issue: 321
  - issue: 322
  - issue: 320
  - issue: 319

  # Release/build work remains important, but it no longer displaces the product
  # loop merely because it is infrastructure. #338 is already complete; the
  # remaining Windows/release work follows once the daily-use surface is coherent.
  - issue: 336
  - issue: 333

  # M5 — Module-Ready Application Boundary.
  - issue: 324
  - issue: 326
  - issue: 327

  # M6/M7 — First usable hosted integration and one proven action.
  - issue: 328
  - issue: 329

  # M8 — Consume source-owned executable readiness without becoming a workflow
  # planner. #352 stays blocked until Streamline exposes a stable ready-work
  # provider contract. Once ready-work admission is stable, #356 groups one
  # Streamline workflow into one TaskCollection without copying its dependency
  # graph or allowing leaf count to inflate top-level scheduling probability.
  - issue: 352
  - issue: 356

  # R2 — Distribution polish after the signed release path is stable.
  - issue: 339

fallback: oldest
